# Test Configuration - Uses real AWS databases with hardcoded values
# PostgreSQL Configuration for Integration Tests
spring.datasource.url=jdbc:postgresql://callcat-sql.c07eqei4o4o7.us-east-1.rds.amazonaws.com:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=huqvCGVVTsW63p0b74Rl
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false

# AWS Configuration for DynamoDB
aws.region=us-east-1
aws.dynamodb.blacklist-table-name=callcat-blacklist

# JWT Configuration for Tests (hardcoded)
jwt.secret=4a718b39e18eb8410c9d82314c5d4d6f3a50447e88d79578f197ba2c1b8e994d
jwt.expiration=86400000

# Email Configuration (Mock for tests)
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=test
spring.mail.password=test
app.email.from=test@callcat.com
app.frontend.url=http://localhost:3000

# Security Configuration
app.security.h2-console.enabled=false

# Logging for tests
logging.level.org.springframework.security=WARN
logging.level.com.callcat=INFO
logging.level.org.hibernate.SQL=WARN